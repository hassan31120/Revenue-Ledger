<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>

        <!--
            The suite runs against a real MySQL database, NOT sqlite.

            The financial invariants in this system are enforced by the database itself:
            CHECK constraints, STORED generated columns used as partial unique indexes,
            BEFORE UPDATE/DELETE triggers on the ledger, and SELECT ... FOR UPDATE row
            locking. sqlite supports none of those faithfully, so a green sqlite suite
            would prove nothing about the behaviour that matters here.
        -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="3306"/>
        <env name="DB_DATABASE" value="instructor_revenue_ledger_test"/>
        <env name="DB_USERNAME" value="root"/>
        <env name="DB_PASSWORD" value=""/>

        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>

        <!-- Deterministic provider behaviour by default; individual tests opt into failure modes. -->
        <env name="PAYOUTS_MOCK_MODE" value="success"/>
        <env name="PAYOUTS_MIN_AMOUNT_MINOR" value="0"/>
    </php>
</phpunit>
